---

- name: Install Podman
  yum:
    name: podman
    state: present

- name: Run the Ubuntu image and install Python3
  command: podman run -d --name rainloop ubuntu:19.10 bash -c "apt-get -qq update -y && apt-get -qq install python3 -y && sleep infinity"
